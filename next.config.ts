/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Calender',       // your repo name
  assetPrefix: '/Calender/',    // ensures assets load correctly
}

module.exports = nextConfig
